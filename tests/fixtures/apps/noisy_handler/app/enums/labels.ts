/**
 * An enum module: a plain array, imported by name. `LABELS.map(cb)` is
 * `Identifier.method(args)` — the very shape `static-service` exists for — so a
 * resolver claims it, resolves this file, and finds no `map` in it.
 */
export const LABELS = ['draft', 'archived'] as const
