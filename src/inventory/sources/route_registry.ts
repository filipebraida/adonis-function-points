/**
 * Fonte: `.adonisjs/client/registry/schema.d.ts` (gerado).
 *
 * Presente nas 6 aplicações levantadas, independente de layout. Entrega, por
 * rota: nome, verbos HTTP, padrão, e os tipos de `body` e `query` inferidos do
 * VineJS — ou seja, os DETs de entrada, sem precisar parsear validator.
 *
 * É a espinha das funções transacionais.
 */
export {}
