/**
 * Fonte: `.adonisjs/server/controllers.ts` (gerado).
 *
 * Mapa nome -> arquivo do controller. Indexar pelo caminho pontuado completo
 * (`collect.web.Invoices`): numa app levantada havia 5 colisões de nome
 * simples entre módulos diferentes, e indexar por nome simples resolvia o
 * controller errado — em silêncio.
 */
export {}
